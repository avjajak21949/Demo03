@model Demo03.Models.Class

@{
    ViewData["Title"] = "Edit Class";
}

<div class="container mt-4">
    <h1>Edit Class</h1>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <input type="hidden" asp-for="ClassID" />
                        
                        <div class="form-group mb-3">
                            <label asp-for="Name" class="control-label">Class Name <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" required />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label asp-for="CourseID" class="control-label">Course <span class="text-danger">*</span></label>
                            <select asp-for="CourseID" class="form-control" asp-items="ViewBag.CourseID" required>
                                <option value="">-- Select Course --</option>
                            </select>
                            <span asp-validation-for="CourseID" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label asp-for="TeacherId" class="control-label">Teacher <span class="text-danger">*</span></label>
                            <select asp-for="TeacherId" class="form-control" asp-items="ViewBag.TeacherId" required>
                                <option value="">-- Select Teacher --</option>
                            </select>
                            <span asp-validation-for="TeacherId" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label asp-for="ScheduleInfo" class="control-label">Schedule Information <span class="text-danger">*</span></label>
                            <input asp-for="ScheduleInfo" class="form-control" placeholder="e.g. Mon-Wed-Fri 9:00-11:00" required />
                            <span asp-validation-for="ScheduleInfo" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label asp-for="MaxCapacity" class="control-label">Maximum Capacity <span class="text-danger">*</span></label>
                            <input asp-for="MaxCapacity" class="form-control" required min="1" />
                            <small class="form-text text-muted">Enter the maximum number of students that can enroll in this class (minimum 1)</small>
                            <span asp-validation-for="MaxCapacity" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                            <button type="submit" class="btn btn-primary">Save Changes</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(function () {
            // Highlight all required fields
            $('form').find('input[required], select[required], textarea[required]').each(function() {
                $(this).siblings('label').addClass('font-weight-bold');
            });
        });
    </script>
}
